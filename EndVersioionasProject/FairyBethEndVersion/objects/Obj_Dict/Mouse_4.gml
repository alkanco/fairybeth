/*if (global.showDictionary) {
    var mx = mouse_x;
    var my = mouse_y;

    // Left Arrow
    if (mx > 120 && mx < 160 && my > 450 && my < 490) {
        if (global.dictPage > 0) {
            global.dictPage -= 1;
        }
    }

    // Right Arrow
    if (mx > 200 && mx < 1300 && my > 100 && my < 1200) {
        show_debug_message("right arrow has been clicked");
        if ((global.dictPage + 1) * global.items_per_page < array_length(global.foundItems)) {
            global.dictPage += 1;
        }
    }
}  */